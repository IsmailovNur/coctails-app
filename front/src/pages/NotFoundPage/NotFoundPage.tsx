import { Typography } from "@mui/material";

export const NotFoundPage = () => {
  return (
    <div>
      <Typography variant="h2" sx={{textAlign: "center"}} >Not Found</Typography>
    </div>
  );
};
